# Streamlit Cloud Secrets 配置示例
# 部署到 Streamlit Cloud 时，把下面这些填到 Streamlit Cloud 的 Secrets 页面
# https://share.streamlit.io → 你的 app → Settings → Secrets

# 后端 API 地址（部署到 Render 后填入 Render 分配的 URL）
MORNDIGEST_API_URL = "https://morndigest-backend.onrender.com"

# 可选：自定义超时
MORNDIGEST_API_TIMEOUT = "60"
